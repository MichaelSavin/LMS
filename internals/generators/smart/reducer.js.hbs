import { fromJS } from 'immutable';
import {
  SOME_ACTION,
} from './constants';

const initialState = fromJS({});

function {{ camelCase name }}Reducer(state = initialState, action) {
  switch (action.type) {
    case SOME_ACTION:
      return state;
    default:
      return state;
  }
}

export default {{ camelCase name }}Reducer;

// Добавить в reducers.js
// import {{ camelCase name }}Reducer from 'components/{{ properCase name }}/reducer';
// { {{ camelCase name }}: {{ camelCase name }}Reducer }
